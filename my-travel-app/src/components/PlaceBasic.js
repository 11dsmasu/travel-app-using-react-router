const PlaceBasic = (props) => {

    // console.log(props)

    return (
        <div className="place-card" > 
            
            
            <h2> { props.province } </h2> 
        </div>
    )
}

export default PlaceBasic